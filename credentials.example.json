{
  "type": "service_account",
  "project_id": "your-project-id",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "your-service-account@your-project.iam.gserviceaccount.com",
  "client_id": "...",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "...",
  "universe_domain": "googleapis.com"
}

# Instructions:
# 1. Create a Google Cloud service account with Sheets + Drive access.
# 2. Download the JSON key file.
# 3. Rename the downloaded file to "credentials.json" (exact name).
# 4. Place it in the same folder as app.py.
# 5. Share your Google Sheet ("MERL Orders - Module 2") with the service account email (edit access).
#
# Never commit the real credentials.json to git — it is already in .gitignore.